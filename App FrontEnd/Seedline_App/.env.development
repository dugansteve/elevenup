# Local development environment variables
# These are only used when running `npm run dev`

# Brand (seedline or elevenup) - defaults to elevenup
VITE_BRAND=elevenup

# Admin server API (for account type lookups during local development)
VITE_ADMIN_API=http://localhost:5050/api

# URL validator service (optional)
VITE_VALIDATOR_URL=http://localhost:3001
